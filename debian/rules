#!/usr/bin/make -f
export QT_SELECT = qt5

%:
	dh $@ --parallel

override_dh_auto_install:
	dh_auto_install --destdir=debian/tmp

override_dh_fixperms:
	dh_fixperms
	chmod a+x debian/tmp/usr/lib/dde-control-center/plugins/bin/notify-monitor
